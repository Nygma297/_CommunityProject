/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { AppRegistry } from 'react-native';
import Kernel from './src'


AppRegistry.registerComponent('ReactNativeBoilerPlate', () => Kernel);
